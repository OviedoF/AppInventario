export default {
    db_uri: 'file:///data/user/0/host.exp.exponent/cache/DocumentPicker/9559e7d5-2abf-45c1-9f15-f0013adf2f10.db',
    asyncStorage: {
        adminPassword: 'adminPassword',
        invSelected: 'invSelected',
        storageDir: 'storageDir',
    }


    // * 1. Mostrar separado el dig verificador 
    // * 2. Agregar label arriba de los inputs CD
    // * 3. Al cambiar datos, borrar los datos de todos los inputs
    // * 4. En vez de mostrar siempre POS, mostrar el que se va cambiando (amarillito)
    // * 5. Agregar el enviar datos desde el cerrar combinación
    // * 6. Limpiar los datos de los inputs al enviar combinación
}